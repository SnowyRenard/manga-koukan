[package]
name = "manga_koukan"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "*", features = [ "derive" ] }

log = "*"
pretty_env_logger = "*"

image = { version = "*", features = [ "avif-native" ] }
rayon = "*"

zip = "*"
tar = "*"
